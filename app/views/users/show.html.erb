<h1><%= @user.name %></h1>

<h3>Events</h3>
<% if @events.any? %>
<ul>
  <% @events.each do |event| %>
    <li><%= link_to event.name, event_path(event) %></li>
  <% end %>
</ul>
<% else %>
No events found
<% end %>
